<!-- .plans/t3-antigravity-publication-20260909.md -->
<!-- record official runtime publication and the remaining personal sign-in gate -->

# Official Antigravity publication checkpoint

This is Group 4 of the approved four-publication reconciliation. Its source universe remains
frozen at `6c583620ff7ad3235b135af7107c0543467eecfa`. This checkpoint records prepared source
and verification evidence, not a merge or completed authenticated acceptance.

## Selected identity and scope

The selected code tree is `6dec0dca7733858abbe872ffc0b41e46504ff6d0`: 98 changed paths above
the preceding Product tree `083d17d3aa1390519c4667cfffc5aafde231eff6`. This document is an
additional path. The publication manifest identifies the exact commit parent and terminal
code-plus-document tree after any required merge-parent reconstruction.

The existing Antigravity provider and instance IDs gain the pinned official ACP 1.1.1 runtime,
isolated personal OAuth, managed installation and custom-runtime selection, explicit legacy-session
transition, account-advertised catalogs, workspace discovery and native subagent batches. Historical
transcripts and legacy settings are retained. No Android client, API-key authentication flow,
business/GCP setup, automatic paid fallback, or alteration of the existing `agy` installation is
included. Provider-independent custom-model metadata belongs to the preceding Product group.

The generic Models/Add custom slug settings controls remain visible and retain their existing
configuration storage/display behavior. The official driver/provider do not read `customModels`
or `customModelMetadata` to build the live catalog. Native account options/models determine that
catalog; the composer consumes the server inventory, not the settings-only custom rows. Cached
noncustom inventory can remain during an unchecked-account restart, but both initial model
selection and turn admission validate against the current native account options. A saved string
does not establish account access or synthesize executable model capabilities.

The source chain preserves original author identity, author date, complete message and trailers,
and adds explicit source/adaptation provenance. It has 16 nonempty upstream contributions,
separate fork regression coverage, and this separate publication record. The 1.1.1 release metadata
lands as a prerequisite before runtime activation; no obsolete release is manufactured as an
intermediate. Later refinements are not attributed wholesale to the original runtime foundation.

## Source allocation

| Source row and exact commit | Retained nonempty contribution |
| --- | --- |
| 283 — `8ea52c8f2f4361e875d72ab30d72d37cfae4d87f` | Reviewed 1.1.1 archive hashes, sizes, executable/harness pairs and exact pin regression. |
| 239 — `06336460c9988f29c71e839c4c9c840c4552e077` | Official runtime, isolated personal OAuth, installation, typed setup RPC/client consumers, account catalog foundation and explicit legacy continuation transition, adapted to the existing durable provider architecture. |
| 249 — `2120fbc185737b71f09de020b95224f9e636d1e5` | Suppress duplicated installation status while retaining a distinct detail. |
| 274 — `0aae1e2ad7ab174004f39746eb4c72b905de932d` | Native legacy workspace `.agent/skills` discovery root. |
| 276 — `e01c153c18a94a2aa33df7115c6cfee1eb09880f` | Verified browser-helper URL forwarding and same-URL duplicate handling. |
| 280 — `f25e4428961982e3935300cfd3c78f7d0fac4ee9` | Bounded 90-second packaged-runtime startup allowance during health/model probes and logout. |
| 281 — `baf67b6e3accd48d510fd0d39a4e6b978a1eea80` | Live config-option account catalog replacement and provider/driver callback wiring. |
| 282 — `eb334ca57448742139fb8ec38fb397c1e51c45c5` | Native 1.1.1 stderr authorization URL interception and redacted background failures. |
| 285 — `f8a14b28ff80a92d8f4df9a9aa53bfacb8195199` | Native child invocation/result classification, MCP exclusion and terminal updates. Generic `task.updated` contracts/projection are a fork prerequisite of this source. |
| 327 — `2675e3c70327719a99af4ae6e53e7b74fb8a9be0` | Launch is not completion; turn-end/replayed batches become idle with historical timeline bypass. |
| 337 — `00f8b7c28056188e3c5630160806a0afe51c9010` | `subagent_batch` identity and shared/native idle presentation without duplicate task detail. |
| 350 — `d487dfbf46be344e818725be70ee04be2436bfb4` | Suppress the known installed/auth-unknown account-check placeholder in the web banner, including custom instances; retain explicit re-authentication and distinct failure diagnostics. |
| 430 — `912276bcd9ca258ad9309727df8e279e5ab698f8` | Host-platform runtime installation fixture with the exact existing atomic activation regression. Remaining fork installer failures/leases are separate regression coverage. |
| 625 — `6349a0e68a958cc51b7b5198683c1d1db88b8d28` | Typed, redacted `session/new` -32603 post-authentication diagnostic. |
| 661 — `c8872fd22aadb8d155f1bedc10576d8314f93e56` | Retain saved personal-account catalog metadata through startup-only health checks. |
| 662 — `ab67795dde448a3608d8689f7ad1975bb977e5a9` | Actual-user-home Gemini skill roots and bounded links into the isolated personal profile. |

No empty source credits are created for equivalent or inapplicable intent. Row 243
(`1e051873094c0c75cd35fef89c90461c22cce76b`) is inapplicable static manifest folding, not the
account-advertised catalog owner. Row 275 (`ef4cc6085e24d6309252412c6dab5482ac11a09f`) has an
already present canonical icon; arbitrary-case normalization and its old picker gate are not
claimed or restored. Row 350 receives credit only for the applicable status-banner suppression.
The fork has no corresponding Antigravity auth-unknown send blocker to remove; backend resume
and legacy reset remain foundation/fork integration, not provenance from that web-only source.
The already excluded browser-background cosmetic change and other deferred Product/client
residuals are not reintroduced. The inherited repaired Core, Clients and Product fixtures remain
authoritative over stale combined-worktree versions.

## Automated and selected-build evidence

The independently selected Antigravity snapshot passed seven affected-package typechecks and
the direct server bundle. Focused verification loaded 25 files / 549 tests: server 21 / 458,
web 2 / 74 and mobile 2 / 17. These overlap earlier ACP/provider receipts and are not additional
unique coverage. These runs used code tree `8927a6ab169d68176dfecda5670dd7a40a2f410d`;
the carried code tree `693333f70d655378490581feb588e488325df5ef` differs only by one formatter-only
line wrap and three inherited predecessor fixture corrections. Its 96-path format check and
94-source comment/lint checks passed. The only later code change is row 350's two-path banner
adaptation. Its one major regression failed before the fix; the five-test existing banner suite,
web typecheck, and exact two-path format/comment/lint checks then passed. No broader campaign
was repeated for that bounded repair. The pinned pnpm frozen-lockfile operation accepted the selected lock without a lock
change or installing dependencies in that validation directory. Direct and virtual workspace
package links resolve into the selected snapshot, not the combined Product/Antigravity checkout.
No full local workspace suite or manual simulator walkthrough was run. Hosted checks belong to
the exact eventual PR head and are not inferred from these local results.

## Live pre-authentication evidence

The primary agent launched one disposable authenticated web environment from the selected snapshot
using the repository's `test-t3-app` workflow. Through the provider settings UI it enabled Antigravity
and installed the managed runtime. The UI reached "Antigravity is available" and subsequently
identified `agy_acp_server_1.1.1`.

The activated Darwin ARM64 release identity was
`fdfa915652cdb7ba8085cc8fffed072cbe009251aa2c951aabdda07a8c28a189`.
The extracted executable and harness SHA-256 values were respectively
`9d900b93031fc42397f88206e14eba4193729bbef631a70b18e7a19631a6dfac` and
`e0a8ef9d80a1ffb178f945159dda33f73d4a5be65516642542352584b834fa2a`.

Starting personal sign-in reached the official Google account chooser through an
`accounts.google.com` PKCE authorization URL. The in-app browser did not open the popup, so the
primary used the application's Copy sign-in link control and opened the validated destination in
Chrome. The account/consent step was handed to the user and was not performed by the agent.
The attempt expired before completion; the setup UI correctly exposed an expired/retry state.

The focused post-banner check ran on code tree `6dec0dca7733858abbe872ffc0b41e46504ff6d0`.
After restart, the installed 1.1.1 runtime remained available and the real "Not authenticated —
Sign in with Google" diagnostic stayed visible with sign-in enabled. The setup view was legible
with local font assets. No new OAuth attempt was started; this does not establish authenticated
response or resume behavior.

No authenticated account, entitlement, model response, tool permission/question, cancellation,
restart/resume, legacy-session transition, or logout success is claimed. Those real-provider checks
remain mandatory before this group merges. Mocks and successful installation do not replace them.
The PR may collect hosted CI before that acceptance, but must not merge until it is complete.

## Isolation and limits

The first disposable-snapshot launch encountered a symlinked pnpm task-state directory. Its
automatic dependency step touched ignored metadata in the integration worktree; that is not claimed
as an untouched cache. Snapshot task/Vite caches and pnpm metadata were then made local without
removing external targets. Selected tracked source/lockfile blobs and the shared Git index remained
unchanged. Some reused font assets were outside the snapshot Vite allowlist, so the first pre-auth
check does not establish visual font parity. Before the focused post-banner check, the two imported
font packages were copied into the disposable snapshot with byte equality verified and their
external targets preserved; the Vite filesystem allowlist was not broadened.

The owned backend and web watcher were stopped after sign-in expired, and their listener ports
were confirmed closed. The expired Google tab was closed. Disposable installed runtime/state was
retained solely for a fast new acceptance attempt when the user is ready; no personal profile or
credential was removed. OAuth URLs and credentials are intentionally absent from this record.
The dirty integration branch remains at its original baseline: private object preparation and
remote publication do not authorize resetting that checkout or updating its local ref/index.

## Live question repair and verification stop

The authenticated disposable run displayed 11 account-advertised models, returned a real response,
and completed a supervised file write after Allow once. A real native question exposed a durable
delivery deadlock: question replies were excluded from the existing approval/interrupt live-control
lane. Cancelling removed the native question; replay then classified its exact no-longer-pending
error as unknown and blocked later commands. These were application failures, not proof of an
official ACP resume failure.

The fork repair routes question answers through that existing ahead-of-cursor lane with per-action
context, and classifies only the typed Antigravity permission-method no-longer-pending response as
terminal stale input. It preserves unknown-failure fencing for other failures. Two major existing-
fixture regressions first failed, then passed: held-turn answer delivery with rejection, duplicate
command handling and isolated action identity; and cancelled-question retirement without false
resolution or blockage of later turns.

Before the user's instruction to stop testing, the full reactor suite passed 84/84, server type
checking exited zero, and two-path formatting, comment checks, lint and diff checks passed (lint
retained six existing unused-parameter warnings; type checking retained informational suggestions).
No verification was running when that instruction was reconciled. Per the user's direction,
remaining local and live checks are waived and no further tests, type checks, builds, linters or
acceptance scripts are run. The repaired code is not claimed as live-retested. Hosted CI remains
unchanged and authoritative; earlier mandatory local/live gate language in this checkpoint records
the prior acceptance plan, superseded by this explicit stop instruction.
