<!-- .plans/t3-product-publication-20260909.md -->
<!-- record product reconciliation scope and verification boundaries -->

# Product reconciliation publication

This is group 3 of the approved four-publication finish plan, following green merged Core and
Clients/media. It retains the frozen upstream ceiling
`6c583620ff7ad3235b135af7107c0543467eecfa`. The PR records the exact published head and hosted
checks; those are not inferred from the local acceptance described here.

## Scope and compatibility

- Native Claude/Codex account limits and reset guidance, pooled account/environment presentation,
  independently loading selected environments, exact-key custom pricing and read-only reset-credit
  information. No CLIProxy/hub credentials or reset mutation are introduced.
- Custom model metadata alongside legacy slug arrays, removal of orphan custom entries, sparse
  project model/script/browser defaults and searchable project scope. Explicit user model choices
  remain authoritative; metadata does not invent provider capabilities.
- A local/desktop first-run wizard with provider readiness, selectable standalone installation/login
  instructions and repository-grouped import. Instructions are not silently executed; real provider
  login is not claimed. Empty import scans can finish onboarding, old payloads without repository
  identity remain usable, and light/dark follow the existing theme.
- Migration `073_ProjectionThreadsActiveOrderKey`, nullable without backfill, plus durable manual
  active ordering and accessible native arrangement. Pin/snooze/settlement precedence is preserved;
  legacy keyless threads keep deterministic ordering until explicitly moved.
- Default-off balancing across already-connected eligible environments, authorized resource reads,
  fresh capacity/weight filtering and overload rejection. It never creates a connection or imports
  cloud discovery.

Official Antigravity setup/auth/runtime/catalog/legacy-transition changes remain group 4. Its pinned
runtime download dependency does not enter Product. Android, cloud/Relay, standalone auto-pull,
per-turn wire accounting and unrelated cosmetic/release churn remain excluded.

## Provenance

Nonempty source adaptations retain original authors, dates, messages and trailers, with exact SHA
and adaptation notes. Fork-specific wiring/fixture repairs and this publication record are separate.
Equivalent sources receive no empty credit commits. Partial sources retain their explicit boundaries:

- Project defaults do not import the obsolete migration that clears automatic defaults.
- History-derived totals/limits do not claim a new live per-turn usage contract.
- Standalone install guidance does not claim hidden terminal execution or credential acceptance.
- Bounded existing import parsers do not claim upstream's streaming parser architecture or equal
  memory cost on unbounded files.

## Automated gates

The independently selected Product snapshot passed all seven affected-package typechecks
(contracts, shared, client-runtime, server, web, mobile and desktop), with local workspace links
and caches and all six corrected Core fixture identities preserved. Pinned pnpm 12.3.4 frozen-lock,
lockfile-only validation left the selected lock unchanged and performed no install or scripts.
Changed-file final formatting/lint/comment and source-chain gates are recorded in the publication
receipt. No full local workspace suite was run.

The consolidated implementation campaign passed 11 focused Product files / 112 tests covering
shared defaults/models/scripts, settings contracts, load balancing/sort, active-order decider,
migration, persistence and projection. Earlier focused receipts include backend 10 files / 93 tests,
selected-target pricing, native storage/pooling and legacy arrangement. Production web build and
iOS export passed after the final Product implementation; a generic iOS SDK native compilation
also passed without launching a simulator. These are combined-tree receipts, distinct from the
independent selected-tree type/static/lock checks and subsequent hosted CI.

A direct selected-module regression additionally confirmed that a scoped Codex allowance such as
Spark cannot initialize or overwrite the main account allowance, preserves the existing main
snapshot identity, and leaves ordinary main-account normalization working. This checks the actual
normalizer/merge exports; it is not a claim of live provider quota refresh.

## Integrated browser evidence and limits

The single isolated authenticated campaign exercised editable custom pricing with reload, searchable
project-scope selection with query reset, sparse project script inheritance and actual deterministic
PTY execution, and default-off balancing controls. The campaign explicitly enabled then disabled a
single eligible environment's weight. Manual thread arrangement and open panels survived reload.

No live two-host capacity-routing claim is made: freshness, weighting, eligibility and overload
behavior are covered by the focused chooser/authorization regressions. Provider account credentials
and limits used deterministic fixtures. No manual iPhone/iPad walkthrough or simulator runtime
download/erase was performed. Owned browser, backend, Electron and fixture processes were stopped.

Publication uses a merge commit after all applicable checks pass, then verifies the exact merged-main
run. Product's acceptance does not waive the separate human personal-Google gate required before
Official Antigravity merges.
